-- https://github.com/chentoast/marks.nvim
-- Shows marks (ma, mb, ...) as signs in the left column - Neovim's built-in
-- marks have no visual gutter indicator on their own, only `:marks`/`'a`.
return {
	"chentoast/marks.nvim",
	event = "VeryLazy",
	opts = {},
}
